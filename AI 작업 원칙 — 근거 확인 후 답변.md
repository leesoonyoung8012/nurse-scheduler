# AI 작업 원칙 — 근거를 확인한 뒤 답한다

> 2026-08-28 사용자 지정. **모든 앱·프로젝트 폴더 공통.**
> "간혹 사실과 다르게 대답하는데, wiki를 확인한 후 정확한 답변을 해줘."

---

## 1. 원칙

**작업할 때뿐 아니라 '질문에 답할 때'도 근거를 직접 열어 확인하고 말한다.
추측이나 기억으로 단정하지 않는다.**

한 파일만 보고 나머지를 추론하는 것이 가장 흔한 실패 원인이다.
파일 하나가 담는 의미는 이름에서 짐작한 것과 다를 때가 많다.

## 2. 확인할 곳 (순서대로)

1. **🏢 업무 vault의 위키** — `~/Desktop/Wiltse_General Affairs`
   - 프로젝트 요약본: `06_AI Innovation TFT/…/<프로젝트>.md`
   - 작업 브리핑: `06_AI Innovation TFT/YYYY-MM-DD … 작업 브리핑.md`
   - **정의가 바뀐 이력은 코드가 아니라 위키에만 있는 경우가 많다.**
2. **그 폴더의 `CLAUDE.md`·`README.md`** — 설계 의도·금기사항
3. **실제 코드·스크립트** — 동작을 말하기 전에 해당 파일을 열어볼 것
4. **로그·데이터·앱에 반영된 값** — "지금 어떤 상태인가"는 로그로 확인
5. 개인 내용은 🏠 개인 vault(`~/Desktop/토리상자(Obsidian)`)에만 — 업무와 섞지 않는다

## 3. 답할 때 지킬 것

- **근거를 함께 밝힌다** — "어느 파일의 어떤 값을 보니 …" 형태로.
- **확인하지 못한 부분은 "확인 안 했다"고 분명히 말한다.** 모르는 것을 아는 것처럼 말하지 않는다.
- 자료가 서로 어긋나거나 정의가 모호하면 **추정하지 말고 먼저 질문한다.**
- 새 산출물은 가능하면 **기존 견본·과거 수치를 재현해 검증한 뒤** 내보낸다.

## 4. 실제로 틀렸던 사례 (2026-08-28, 수술통계앱)

같은 날 두 번 틀렸고, 원인은 둘 다 "파일을 안 열어보고 추론한 것"이었다.

| 잘못된 답 | 사실 | 무엇을 봤어야 했나 |
|---|---|---|
| "데이터가 8/25에서 멈춰 있다" | 8/26분이 이미 반영돼 있었다 | `반영이력.json`의 `dataThrough`는 **메일 송부에 성공한 시점** 기준. 반영 여부는 `가공업로드.log`의 `[요약]` 줄로 판단해야 한다 |
| "버튼 1은 구글 메일에서 받아온다" | **다운로드 폴더**에서 가져온다 (구글은 0번 무인실행·7번 따라잡기만) | `1_가공하고 업로드까지.command` 첫 20줄만 열어봤으면 알 수 있었다 |

**교훈: 파일 이름·기억으로 단정하지 말고, `cat`·`grep`으로 실제 내용을 확인한 뒤 답한다.**

---

*이 파일은 데스크탑의 모든 앱 폴더와 `~/! AI agent 구축`(AX-관제실 포함)에 동일하게 들어 있다. 내용을 고칠 때는 전체를 함께 갱신할 것.*
