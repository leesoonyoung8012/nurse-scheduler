# 작업 인계 — 2026-09-03 (저녁 갱신: 개선 계획 1~12 전 항목 완료·배포)

> 새 세션은 이 문서를 먼저 읽고 이어서 작업할 것.
> 프로젝트 `CLAUDE.md` · `reference/deploy-checklist.md` · `.claude/rules/` 는 그대로 적용된다.

---

## 0. 큰 목표 (사용자 지시)

> 병동 수간호사들이 "규칙과 개인조건에 100% 부합하는 근무표가 자동생성되지 않는다"는
> 불만으로 이 앱을 쓰지 않는다. **이 앱을 쓸 수밖에 없게 만드는 것**이 목표.
> **"써보게 할 때마다 안 된다는 느낌을 받으면 안 된다"** — 시뮬레이션 수치 확인 후 배포.

---

## 1. 배포 상태

**2026-09-03 저녁, 개선 계획 1~12 전 항목을 구현하고 양 사이트에 배포 완료.**
(`firebase deploy` release complete — ai-duty-lsy.web.app · lsy-duty-f8012.web.app)

- 배포 확인 마커: `curl -s https://ai-duty-lsy.web.app | grep -c "phase6h_alignTraineeWithPreceptor"`
  → 0이면 미배포. (배포 직후 세션에서는 curl 권한 제한으로 이 확인만 못 했음 — 손으로 1회 확인 권장)
- **커밋은 아직 안 함** — 사용자 "작업완료" 대기. 변경 파일:
  `public/index.html` · `CLAUDE.md` · `reference/api-convention.md` · `작업인계_2026-09-03.md`
  (+ 저장소 밖 `~/nurse-scheduler-harness/verify.js`)

## 2. 현재 수치 (새 기준선)

`~/nurse-scheduler-harness/sim2.js` — 61W·43W 각 6개월, 절대검증기 통과 후

```
                 미달     하드위반   검증기삭제
어제 배포본       237건       0건        1건
오늘 배포본       199건       0건        1건     (61W 60→34 · 43W 177→165)
```

- 전 병동 36개월(`sim.js`): 엔진 갭 127 → **70건**
- 신규-프리셉터 불일치(6개월): 61W 133→52일 · ICU 129→73일 (ICU 미달 24→5)
- N 개수 표준편차(N 가능자 기준): ICU 0.80→0.58 · 43W 0.80→0.76 · **61W 0.62→0.81 (악화)**
  — 61W는 N 배치량이 크게 늘며(미달 43→22) 제약 여유가 있는 직원에게 추가 N이 몰린 트레이드오프.
- `verify.js` 35개 검사 전부 통과. `compare.js` 기준은 어제 이전 원본이라 "달라짐"이 정상.

## 3. 오늘 완료한 개선 계획 (1~12)

| # | 내용 | 핵심 |
|---|---|---|
| 1 | 개인조건 미반영 버그 | `_shiftTypeEnabled` 폐기(읽기·쓰기·저장 제거). 김정아 사례 해소 |
| 2 | 개인조건 기본값 | 근무유형 선택 시 허용 근무 전부 켜짐. 기존 빈 데이터 46명 이관(`normalizeWardStructure`). 요약에 "미사용" 표기 |
| 3 | 회복 OFF 하한 | `getPreviousMonthNightRecoveryRemaining` 하한 2→1 (병동 설정 존중) |
| 4 | 프리셉터 동반 | `scoreAssignment` 가중치 적용 + `phase6h_alignTraineeWithPreceptor` 신설 (N은 블록 단위 정렬, snapshot 복구) |
| 5 | 홀수 N 잔여 1개 | `_fillOneNightCell` 전략3 — 2+블록 보유·잔여 쿼터 정확히 1일 때만 크기 1 허용 |
| 6 | N 공평 배분 | phase3 라운드 방식 (N 적은 순 + 차지 우선 타이브레이크) |
| 7 | 최소 1 단일 N 남발 | phase3 크기 1 폴백은 잔여 쿼터 1일 때만. what-if 단일 N 8→7, 삭제 3→1 |
| 8 | 근무형태 경고 | 최소=최대·정원 부족을 저장 화면에 경고 배너 (diagnose 재사용) |
| 9 | 신청 정책 강제 | `checkLeavePolicy` — 관리자는 확인창, 모바일은 거부(기존), 목록 "정책 초과" 배지 |
| 10 | 수동 입력 5조 차단 | 셀 편집 되돌림+드롭다운 필터, 엑셀 업로드 OF 비움+목록, reportOnly 검증기도 5조만 실제 제거(`removedHard`) |
| 11 | 충돌 개인조건 차단 | `personalConditionConflicts` a~f — 조건창 비활성+사유, 저장 3경로 거부, 명단 빨간 표시, 생성 전 검사 차단 |
| 12 | N 재진입 텀 병동 설정 | `ward.nightReentryGap` (미설정=근무유형 값) + 병동 화면 입력칸 |

부수 결함 수정 (5·6·7 작업 중 발견):
- **canAssign에 8조(듀티 최대인원) 게이트가 없었다** → 추가. phase6b `tryMove`는 B를 먼저 빼고 검사하도록 순서 교정.
- 비시니어 N이 시니어 없는 날에 얹혀 7조 검증기가 통째로 지우던 낭비 → `_nightSeniorJoinOK` 가드를
  planNightBlockBestFit·_fillOneNightCell·phase3d에 적용 (차지가능=시니어 판정 동일 확인).

## 4. 배포 후 유의점 (수간호사 문의 대비)

1. **시니어(LV3) 0명 병동(63W·교육전담·간호부)은 이제 생성 버튼이 사전검사에서 막힌다** (7조).
   근무자 명단에서 등급을 올려야 진행 가능 — 의도된 동작.
2. **저장된 옛 근무표를 다시 저장하면 5조 위반 셀이 OF로 비워진다**
   (2026-08 기준: 43W 44셀 · 61W 34셀 · ICU 2셀 — 대부분 MD 근무유형 미허용). 기존 데이터 자체는 안 건드림.
3. 개인조건이 대원칙과 충돌하면 저장·생성이 막히고 명단에 빨갛게 표시된다. 현재 데이터에는 충돌 0건.
4. 43W 미달 165건은 여전히 **설정 문제**(월 나이트 최대 합계 130 < 필요 155) — 수간호사 판단 사항, AI가 임의 조정 금지.
5. 61W N 개수 편차가 다소 커졌다(0.62→0.81). 불만이 나오면 라운드 정렬 기준 조정 여지 있음.

## 5. 도구

**검증 하니스**: `~/nurse-scheduler-harness/` — sim2.js(주 지표) · sim.js · compare.js · verify.js · probe.js · pipeline.js
실데이터: `~/NurseSchedulerBackups/` 최신 백업. 네트워크·인증 불필요.
`compare.js`·`index_원본_2026-09-03.html` 기준은 **어제 작업 전**이므로 지금은 달라짐이 정상.

**요청 감시 자동화** (정상 작동 중): `com.nurse-duty.request-alert`(5분) · `com.nurse-duty.request-brief`(16:00)

## 6. 미해결 · 알고만 있을 것

| | 내용 |
|---|---|
| 63W | 시니어 0명 + 인원 7명으로 필요 155 > 가용 147. 데이터·인력 문제 |
| 빈 병동 5곳 | 31W·33W·41W·51W·응급간호 — 간호사 0명 |
| 죽은 코드 | 레거시 `generateSchedule`(~2064) · `runGenerator` · step* 체인. **건드리지 말 것** |
| 전체 덮어쓰기 저장 | `saveData()`가 노드 전체 `.set()` — 11명 동시 사용 시 마지막이 이김 |
| 하드코딩 코드 배열 | 근무코드 32곳·휴무코드 40곳 |
| 요청함 cr1 | 시드 데이터 — 앱 > 요청함에서 삭제하면 되살아나지 않음 (미삭제 상태) |
