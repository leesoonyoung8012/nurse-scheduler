# 근무자 명단 및 근무 형태 — 근무표 생성 기준

## 1. 차지가능 태그 및 듀티 배치 (하드 규칙)

- D/E/N 각 듀티마다 **차지가능 태그**가 부여된 직원이 반드시 1명 이상 배치되어야 한다.
- 차지가능 여부는 오직 **차지가능 태그** 하나로만 판정한다. 아래 항목은 대체 조건으로 인정하지 않는다.
  - LV3·LV4·LV5 등 레벨만으로는 차지 가능자로 보지 않는다.
  - LV5(책임/수간호사)도 차지가능 태그가 없으면 차지 가능자가 아니다.
  - 프리셉터 태그, 교육담당 태그도 차지가능의 대체 조건이 아니다.
- 차지가능 태그는 수간호사가 시스템에서 직원별로 직접 지정하는 별도 역할 분류다.
- 차지가능 태그 보유자가 없는 듀티는 절대 허용하지 않는다.
- 배치가 불가능한 경우 해당 칸을 빈 칸으로 남기고 위반 경고를 표시한다 (억지로 채우지 않는다).

## 2. 근무 조건 전체 준수 (하드 규칙)

아래 조건은 선택이 아니라 **모두 동시에** 반영되어야 한다. 어느 하나라도 위반이 되면 해당 배치는 성립하지 않는다.

| 조건 항목 | 내용 |
|-----------|------|
| 근무유형 | 허용된 근무유형(3교대·나이트킵·상근 등)에 맞게 배치 |
| 개인조건 | 허용된 근무 종류(D/E/N)와 허용 요일만 배치 |
| N 최대 개수 | 월 나이트 최대 개수 초과 금지 |
| 연속근무 최대 | 설정된 최대 연속근무 일수 초과 금지 |
| 연속 N | 설정된 최소~최대 연속 나이트 범위 준수 |
| 연속 OFF | 설정된 최소~최대 연속 OFF 범위 준수 |

## 3. 나이트 사이클 배치

- 나이트는 월 **2사이클** 배치를 원칙으로 한다.
- 나이트 텀(재진입 간격)이 너무 짧아지지 않도록 한다.
- 나이트를 1개씩 부여하여 사이클이 3회 이상이 되지 않도록 한다.
- 단, 부득이한 경우 **3사이클까지 예외 허용** (소프트 규칙).

## 4. 나이트 후 2일 OFF (하드 규칙)

- 1사이클 = 연속 N (설정된 최소~최대 개수) + **반드시 2일 OFF**
- N 블록 종료 후 2일 이내에 D/E 배치 절대 금지.

## 5. N 다음날 D/E 금지 (하드 규칙)

- 나이트 근무 다음날에는 D/E 근무를 배치하지 않는다.

## 6. E 후 D 금지 (하드 규칙)

- E 근무 다음날에는 D 근무를 배치하지 않는다.

## 7. 원티드 OFF 준수 (하드 규칙)

- WO(Wanted Off)는 반드시 지켜준다.
- WO를 침범해야 하는 상황이 생기면 WO를 변경하는 것이 아니라, 일반 OFF 취소 → D/E 재배정 → N 재배정 순으로 해결한다.

## 8. 신규-프리셉터 동반 배치

- 신규교육중인 직원은 매칭된 프리셉터와 동일 듀티로 배치한다.
- 신규교육중인 직원의 WO는 반영하며, 이 경우 프리셉터를 따라갈 필요 없다.
- 프리셉터가 WO인 날에는 신규교육중인 직원이 따라갈 필요 없다.

## 9. D/E/N 균등 배분 (소프트 규칙)

- 개인조건이 별도로 규정되지 않은 3교대 간호사는 D/E/N 근무가 가능하면 균등하게 배치한다.

## 10. 최소인원 우선, 초과 OFF 시 추가 배치

- 근무는 최소인원 기준에 맞추어 우선 배치한다.
- 배치 후 오프 수가 기본 오프 수보다 많으면 D → E → N 순으로 추가 배치해 본다.

## 11. 전월 연속성 유지 (하드 규칙)

- 1개월 단위 근무표이지만 전월의 연장선으로 생성한다.
- 위 1~10 항목의 기준이 전월 말일에서 당월 1일로 이어져 연속 적용된다.
- 전월 마지막 N 블록 후 회복기, 연속근무 카운트, E→D 금지 등이 모두 이어진다.

## 12. 배치 불가 시 빈 칸 처리 (하드 규칙)

- 1번·2번 항목을 반영했을 때 조건에 맞는 근무자가 없으면 **억지로 채우지 않는다**.
- 어떠한 조건에도 위반이 되는 상황이면 해당 듀티 칸을 **빈 칸**으로 남긴다.
- 빈 칸은 수간호사가 확인하여 수동으로 처리한다.
