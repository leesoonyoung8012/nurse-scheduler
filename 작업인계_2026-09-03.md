# 작업 인계 — 2026-09-03

> 새 세션은 이 문서를 먼저 읽고 이어서 작업할 것.
> 프로젝트 `CLAUDE.md` · `reference/deploy-checklist.md` · `.claude/rules/` 는 그대로 적용된다.

---

## 0. 큰 목표 (사용자 지시)

> 병동 수간호사들이 "규칙과 개인조건에 100% 부합하는 근무표가 자동생성되지 않는다"는
> 불만으로 이 앱을 쓰지 않는다. **이 앱을 쓸 수밖에 없게 만드는 것**이 목표.

추가 지시 (2026-09-03):

> **"써보게 할 때마다 안 된다는 느낌을 받으면 안 된다.
> 충분히 개선하고 시뮬레이션도 몇 차례 돌려보면서 완벽에 가깝게 수정하고 배포해야 한다."**

→ 어설픈 상태로 수간호사에게 내보내지 말 것. 시뮬레이션으로 수치를 확인한 뒤 배포한다.

---

## 1. ⚠️ 배포 상태 — 가장 먼저 확인할 것

`public/index.html` 에 **커밋도 배포도 안 된 변경 356줄**이 있다.

```bash
git status --short     # M public/index.html (+356/-67)
```

| 항목 | 로컬 | 배포됨 |
|---|:--:|:--:|
| 근무코드 MD(09:00~18:00) · 교육 · 공가 | ✅ | ✅ |
| MD·SD6 자동배정 (phase4 일반화) | ✅ | ✅ |
| 개인조건에 MD·SD6 포함 | ✅ | ✅ |
| 요청함 보강 (상태 6단계·답글·분류) | ✅ | ✅ |
| 시드 요청(cr1) 제거 | ✅ | ✅ |
| 생성 전 가능성 진단 패널 | ✅ | ✅ |
| **N 재진입 텀 → 블록 플래너 (양방향)** | ✅ | ❌ |
| **신규를 정원 외로 (최대인원 계수 통일)** | ✅ | ❌ |
| **phase5c_fillShortages (미달 겨냥 보충)** | ✅ | ❌ |

배포 확인법: `curl -s https://ai-duty-lsy.web.app | grep -c "phase5c_fillShortages"`
→ `0` 이면 미배포, `2` 면 배포됨.

**커밋은 사용자가 "작업완료"라고 할 때만.** 오늘 하지 않았다.

---

## 2. 현재 수치 (다음 작업의 기준선)

`~/nurse-scheduler-harness/sim2.js` — 61W·43W 각 6개월, **절대검증기 통과 후** 기준

```
                미달     하드위반   검증기삭제
오늘 시작        327건       0건       22건
현재            237건       0건        1건      (-28%)
```

전 병동 36개월분 (`sim.js`): 엔진 갭 218 → **127건**
24개 병동-월 전수 검사: **대원칙 위반 0건 · 빈칸 0건**

### 남은 237건의 정체

```
43W  177건  전부 N — 월 나이트 최대 합계 130 < 필요 155   ← 설정 문제. 엔진으로 해결 불가
61W   43건  전부 N — 정원 여유 +4로 빠듯 (미사용 상한 11)  ← 일부는 엔진 갭
61W   17건  D/E                                          ← 잔여 엔진 갭
```

**D/E는 거의 해결됐고 남은 건 대부분 N이다.**

---

## 3. 다음에 할 일 (우선순위)

### 3-1. N 보충 파기 ← 여기서 멈춤

61W는 월 N 상한을 11회 남기고도 7~9건 미달이다. 왜 그 여유를 못 쓰는지 추적할 것.

```bash
cd ~/nurse-scheduler-harness
node probe.js 61W 10      # 미달 슬롯별 거부 사유
node pipeline.js 61W 10   # phase 단계별 미달 추이
```

의심 지점 — N은 블록 단위라 낱개로 못 넣는다. `minConsecutiveNight`(기본 2) 때문에
1칸 부족한 날에 블록을 못 만들어 통째로 포기하는 것으로 보인다.
`_fillOneNightCell` 전략2가 `minSize < 2` 인 사람을 건너뛴다 (단일 N 사이클 방지).

**주의**: CLAUDE.md 엔진 불변식 — N 낱개 배정 경로를 새로 만들지 말 것.
블록 확장 또는 `_fillOneNightCell` 을 쓸 것.

### 3-2. 43W 설정 판단 (사용자 몫)

진단 패널이 이미 해결책을 숫자로 제시한다. 수간호사가 판단할 값이라
**개인별 월 나이트 최대를 AI가 임의로 올리지 말 것** (사용자 지시).
이게 반영되면 43W 177건이 대부분 사라져 엔진 작업보다 효과가 크다.

### 3-3. 진단 패널 문구 재검토

현재 43W·63W는 열자마자 빨간 "완성할 수 없습니다"가 뜬다.
사용자 지시("안 된다는 느낌을 주면 안 된다")와 충돌할 소지가 있다.
**"이렇게 하면 됩니다" 로 바꾸고 원클릭 적용 버튼을 붙이는 안**을 논의하다 중단됨.

### 3-4. 집중간호(ICU)는 별도

사용자 지시: "ICU는 근무 배치 기준이 좀 다를 수 있어 별도로 시뮬레이션할 것."
61W·43W 기준으로 먼저 맞추고, ICU는 따로 볼 것.

---

## 4. 확정된 정책 결정 (사용자 승인)

| 결정 | 내용 |
|---|---|
| 교육·공가 | 휴무 계열 · 신청/수동 전용 (자동생성이 배정하지 않고 보호만) · 한글 표기 |
| MD | 09:00~18:00 근무코드. 자동배정은 **개인별 설정**으로 제어 |
| 신규 간호사 | **정원 외** — 최소·최대 인원 모두에서 제외 (프리셉터 동반 견습) |
| 43W 나이트 상한 | 수간호사가 직접 판단. AI가 임의 조정 금지 |
| 요청 처리 절차 | 오류신고=5분 알림 즉시 대응 / 나머지=16시 브리핑 → 컨펌 → 수정 |
| 컨펌 방식 | 요청함에서 상태를 **"수정중"** 으로 바꾸는 것이 곧 승인 |

---

## 5. 오늘 고친 것 (원인까지)

1. **N 재진입 텀이 절대검증기에만 있었다** — 엔진이 위반을 만들고 검증기가 지우는 낭비.
   `_blockStreakOK` 로 옮김. 처음엔 효과 0이었는데 **앞쪽만 보던 것**이 원인이었다
   (나중에 앞에 놓인 블록이 이미 놓인 뒤 블록의 텀을 깼다). 앞뒤 양방향으로 고쳐 삭제 22→1.

2. **최소/최대 인원 계수 비대칭** — 최소는 신규 제외, 최대는 신규 포함이었다.
   61W(평일 D 최소5/최대5)에서 신규 1명이 D에 들어가면 그 듀티는 **영구 미달**이었다.
   미달 25건 중 18건이 이 탓. 정원 외로 통일해 해소.

3. **phase5b가 부족 듀티를 겨냥하지 않았다** — 날짜는 부족순으로 고르면서 배정은
   `['D','E']` 중 먼저 되는 쪽에 넣어, E가 모자란 날 D에 사람을 넣었다. `phase5c` 신설.

4. MD·SD6 자동배정 부재 — `canAssign` 은 통과시키는데 배정하는 phase가 없어
   MD/SD6 전담을 지정하면 **한 달 내내 OF** 가 나왔다. phase4를 일반화해 해결.

5. 개인조건이 MD·SD6를 무시 — `hardPlacementAllowedForNurse` 와
   `normalizePersonalShiftDayRules` 가 D/E/N만 훑어, MD/SD6 개인규칙이
   **저장할 때마다 삭제**되고 요일 제한도 안 걸렸다.

---

## 6. 미해결 · 알고만 있을 것

| | 내용 |
|---|---|
| 63W·교육전담·간호부 | **시니어(LV3) 0명** → 배정이 전량 삭제됨. 화면 "LV3 시니어"=내부 `LV4`. 데이터 문제이며 앱에서 등급을 올려야 함 |
| 63W | 인원 7명으로 필요 슬롯 155 > 가용 147. 수학적으로 불가 |
| 빈 병동 5곳 | 31W·33W·41W·51W·응급간호 — 간호사 0명 |
| 죽은 코드 | 레거시 `generateSchedule`(2064~3800) · `runGenerator` · `runAdminGenerator` · `step*` 마무리 체인. 어디서도 호출 안 됨. **건드리지 말 것** (CLAUDE.md) |
| 전체 덮어쓰기 저장 | `saveData()` 가 `nurseScheduler` 노드 전체를 `.set()`. 11명 동시 사용 시 마지막이 이김 |
| 하드코딩 코드 배열 | 근무코드 32곳 · 휴무코드 40곳. 코드 추가 때마다 함정 |
| `watchFirebaseData` | `ensureDataDefaults()` 만 부르고 `ensureShiftCodeWorkTypeConsistency()` 는 안 부름 (기존 불일치) |
| 요청함 cr1 | 시드 데이터. 코드는 배포됐으니 **앱 > 요청함에서 삭제**하면 되살아나지 않음 (미삭제 상태) |

---

## 7. 도구

**검증 하니스**: `~/nurse-scheduler-harness/` (저장소 밖 — testing.md 규약)
실데이터는 `~/NurseSchedulerBackups/` 최신 백업 JSON. 네트워크·인증 불필요.

```bash
cd ~/nurse-scheduler-harness
node sim2.js       # 주 지표 — 61W·43W 6개월, 검증기 통과 후
node sim.js        # 전 병동 36개월
node compare.js    # 원본(index_원본_2026-09-03.html) 대비 회귀
node probe.js 61W 10
node pipeline.js 61W 10
./read-requests.sh # 운영 DB 요청 목록 (읽기 전용)
```

`compare.js` 의 기준은 **오늘 작업 전 원본**이라 지금은 "달라짐"이 정상이다
(신규 정원 외·phase5c 등 의도한 변경). **의도치 않은** 변화를 잡는 용도로 쓸 것.

**요청 감시 자동화** (등록·검증 완료, 정상 작동 중)

```
com.nurse-duty.request-alert   5분     새 오류신고 → 맥 알림
com.nurse-duty.request-brief   16:00   미처리 요청 → ~/NurseSchedulerRequests/요청브리핑-*.md
스크립트  scripts/request-watch.sh      로그  ~/Library/Logs/nurse-duty-request.log
```

---

## 8. 배포할 때

`reference/deploy-checklist.md` 를 그대로 탈 것. 요약:

```bash
node ~/nurse-scheduler-harness/sim2.js        # 하드위반 0 · 빈칸 0 확인
firebase login:list                            # leesoonyoung8012@gmail.com
firebase deploy --only hosting:ai-duty-lsy,hosting:main --project lsy-duty-f8012
curl -s https://ai-duty-lsy.web.app | grep -c "<이번 변경의 고유 문자열>"
```

⚠️ 운영 DB에 대한 파괴적 쓰기(DELETE 등)는 자동 승인이 차단된다. 사용자가 앱에서 하거나
직접 실행해야 한다.
