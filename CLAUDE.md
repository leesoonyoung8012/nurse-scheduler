# 코딩 작업 기준

> Andrej Karpathy의 LLM 코딩 원칙을 기반으로 이 프로젝트에 맞게 구체화한 기준입니다.

## 1. Think Before Coding
- 가정이 생기면 바로 코딩하지 말고 먼저 질문할 것
- 모호한 요구사항은 명확히 확인한 후 진행
- 여러 해석이 가능하면 각각 제시하고 선택을 받을 것
- 트레이드오프가 있으면 반드시 언급할 것
- 불명확한 점이 있으면 멈추고 무엇이 불명확한지 구체적으로 명시할 것
- 더 단순한 방법이 있으면 침묵하지 말고 반드시 제안할 것

## 2. Simplicity First
- 가장 단순한 해결책을 우선으로 선택할 것
- 불필요한 추상화, 과도한 레이어, 사용하지 않을 기능 추가 금지
- 100줄로 될 것을 1000줄로 만들지 말 것
- 요청하지 않은 유연성·확장성·설정값 추가 금지
- 발생 불가능한 시나리오에 대한 에러 처리 금지
- 판단 기준: "시니어 엔지니어가 보면 과복잡하다고 할까?" → 그렇다면 단순화

## 3. Surgical Changes
- 요청받은 부분만 정확하게 수정할 것
- 관련 없는 코드, 주석, 스타일은 절대 건드리지 말 것
- 기존 스타일이 마음에 안 들어도 맞춰서 작성할 것
- 이해하지 못한 코드는 수정하지 말 것
- 변경 범위를 작업 전에 명시할 것
- **내 변경으로 고아가 된 import/변수/함수는 직접 제거할 것**
- **기존에 있던 죽은 코드는 건드리지 말고, 발견 시 언급만 할 것**
- 검증 기준: 변경된 모든 줄이 요청 사항에서 직접 추적 가능해야 함

## 4. Goal-Driven Execution
- 구현 전에 계획과 성공 기준을 먼저 제시할 것
- "완료"가 무엇인지 명확히 정의한 후 시작
- 가능하면 테스트 조건을 먼저 정의하고 구현
- 다단계 작업은 아래 형식으로 계획 먼저 제시:
  ```
  1. [단계] → 검증: [확인 방법]
  2. [단계] → 검증: [확인 방법]
  ```

---

# 근무표 생성 규칙 (스케줄러 도메인 지식)

규칙은 **강제력이 다른 4개 층**으로 이뤄진다. 충돌하면 아래 층부터 양보한다.
새 규칙을 넣을 때는 먼저 어느 층인지 정할 것.

## 1층 · 대원칙 8조 (2026-08-12 재정립)

`canAssign()`이 생성 중 차단하고, 생성 직후 절대검증기가 위반 셀을 **제거**한다.
제거 시 사유를 `details`에 남겨 점검 결과 패널에 표시한다.

1. WO·휴가 신청은 근무로 덮이지 않는다
2. 나이트 다음날 D/E 금지
3. E 다음날 D 금지
4. 나이트 묶음 뒤 보호 OFF 확보 — 병동 설정 개수(1~2, 기본 2), 전월 이월분 포함.
   **보호 OFF가 1개면 다음날 D 금지(E부터 가능), 2개 이상이면 D 허용.**
   다음 N 묶음까지의 재진입 텀(근무유형의 `nightReentryGap`)도 이 조에 포함
5. 개인의 근무유형·개인조건(요일 제한 등)을 벗어난 배정 금지
6. 개인별 상한을 넘지 않는다 — 연속근무 최대(기본 5) · 월 나이트 최대 · 연속 나이트 최대
7. **모든 듀티(D/E/N)에 시니어(LV3, 내부 LV4+) 1명 이상.** 구 "중간(LV2) 이상 1명" 규칙은 여기에 흡수됨
8. 듀티별 최대 인원 초과 금지

**헌법에서 뺀 것** — "단독 1N 사이클 금지"는 개인조건 `minConsecutiveNight`를 1로 둘 수 있어
상충하므로 하드에서 제거(2026-08-12). 2사이클 원칙은 4층 소프트로만 유지.

## 2층 · 개인조건

근무유형 · 개인 조건 · 연속근무 최대 · 월 N 최대 · 연속 N 최대 = **하드(1층 5·6조로 강제)**
연속 N 최소 · **연속 OFF 최소/최대 = 소프트** (canAssign에 없음, phase6b 사후 교정만)

- 나이트킵은 월 N이 `ceil(월일수/2)`로 **고정**되고 개인 `maxNightPerMonth`를 읽지 않는다.
  화면도 입력칸 대신 "NK 자동계산"을 표시하므로 설계대로다 (버그 아님)

## 3층 · 병동 기준

- 듀티별 **최소** 인원 = **목표** (개인 상한에 막히면 미달로 남기고 경고) ← 가장 먼저 양보됨
- 듀티별 **최대** 인원 = 하드(1층 8조)
- N 후 보호 OFF 최소 = 하드 / 최대 = 소프트
- 시니어 배치 최소 = 하드(하한 1 고정, 0으로 못 내림) / 최대 = 소프트
- 팀별 구성 = 소프트. 켜면 레벨 균형 자동 교정(phase6d) 대신 팀 커버리지 기준

## 4층 · 소프트 원칙

- 나이트 2사이클 원칙(1개씩 부여해 사이클 3회 이상 지양, 부득이하면 3회 인정)
- 신규-프리셉터 동반 배치 (WO면 따라가지 않음)
- 개인조건 없는 3교대는 D/E/N 균등 배분
- OFF 공평 배분 / 전월 이어 연속성 유지
- OFF 초과 시 수간호사가 수동으로 D→E→N 배치 (프로그램 자동 처리 안 함)

## 구분 체계 (2026-08-09)

수간호사/관리(인원 산정 제외) · 신규 · LV1 주니어 · LV2 중간 · **LV3 시니어(=차지 커버)**
화면 표기와 내부 코드가 한 칸 어긋난다: 화면 "LV3 시니어" = 내부 `LV4`(구 LV5 포함).
`isNurseSeniorSupport()` = 랭크 4 이상.

## 수동 편집은 대원칙을 강제하지 않는다

저장 시 절대검증기는 `reportOnly:true`로 돌아 **위반을 집계만 하고 근무표를 고치지 않는다.**
손으로 고친 근무표는 WO 덮어쓰기·허용 외 코드·연속근무 초과가 그대로 저장된다 (의도된 동작).

---

## 관련 위키 (Obsidian vault — 수정 전 참고)
- 이 프로그램의 설계·운영·주의사항 요약본이 Obsidian vault에 정리돼 있다:
  `/Users/soonyounglee/Desktop/Wiltse_General Affairs/06_AI Innovation TFT/근무·인력/nurse-scheduler.md`
- 작업/수정 전에 위 페이지를 먼저 읽어 설계 의도·주의점을 파악할 것.
- 구조·기능에 큰 변경이 생기면 위 위키 페이지도 함께 갱신할 것.
- vault 전체를 검색하려면 세션에서 `/add-dir /Users/soonyounglee/Desktop/Wiltse_General Affairs`.

---

# 참조 문서 (reference/)

- **데이터 구조(Firebase RTDB)를 읽거나 바꾸는 작업 시 참고**: `reference/api-convention.md`
  — DB 노드 구조, 날짜/코드 규약, 하위 호환 원칙.
- **배포 전 참고**: `reference/deploy-checklist.md`
  — 검증 게이트, 배포 계정/명령, 배포 후 확인, 롤백 플랜.
- 코드 스타일·보안·테스트 규칙은 `.claude/rules/`에 있으며 해당 파일 작업 시 자동 적용된다.
